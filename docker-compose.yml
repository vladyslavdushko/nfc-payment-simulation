services:
  backend:
    build: ./back
    container_name: nfc-backend
    ports:
      - "8000:8000"
    environment:
      - MONGO_URI=${MONGO_URI:-mongodb+srv://vladyslavdusko_db_user:SM8tbv5R6zRJmvKS@payment.vrplirm.mongodb.net/}
      - CI=false
    restart: unless-stopped

  frontend:
    build: ./web/dashboard
    container_name: nfc-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

networks:
  default:
    name: nfc-network